import { Playlists } from "../../types"

type Props = {
  playlist: Playlists;
}

const PodPlayItem = ({ playlist }: Props) => {
  return (
    <div>PodPlayItem</div>
  )
}

export default PodPlayItem